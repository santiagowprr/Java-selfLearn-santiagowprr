package com.santiago;

public class Main {

    public static void main(String[] args) {
        //OPERANDS OPERATORS AND EXPRESSIONS

        // int myVar = 15 + 12; // '+' is an operator, '15' and '12' are operands, '15 + 12' is the expression

//        int result = 1 + 2; // 1 + 2 = 3, /'=' -> assign operator
//        int result2 = 3; // '3' is a literal number here
//
//        System.out.println("1 + 2 = " + result);
//
//        int previousResult = result;
//        System.out.println("previousResult = " + previousResult);
//        result = result - 1;
//
//        System.out.println("3 - 1 = " + result);
//
//        result = result * 10;  // 2*10 = 20
//        System.out.println("2*10= " + result);

        //result and previousResult are different even though we assigned them together. That's because it's copied by value, not by reference


        // ABBREVIATIONS
        // result = result + 1;  we can say like this:
        // result++;

        // result--

        // result = result + 2; we can say like this:
        // result += 2;

        // result = result * 10;
        // result *= 10;

        // result = result / 3;
        // result /= 3;


        // IF / ELSE STATEMENTS

//        boolean isAlien = false;
//        if (isAlien == false) {     // we dont put a semicolon on this line! (after the condition). If we had more than 1 line below the condition statement we'd ned to use the curly braces {}
//            System.out.println("it is not an alien!");
//            System.out.println("I'm scared of aliens!");


//        int topScore = 100;
//        if (topScore != 100) {
//            System.out.println("You got the high score!");

//        int topScore = 100;
//        if (topScore <= 100) {
//            System.out.println("You got the high score!");

//        int topScore = 100;
//        if (topScore >= 100) {
//            System.out.println("You got the high score!");

//        int topScore = 100;
//        if (topScore != 100) {
//            System.out.println("You got the high score!");
//        }

//        int secondTopScore = 60;
//        if ((topScore > secondTopScore) && (topScore < 100)) { // '&&' are ampersands. It is a logical AND operator.
//            System.out.println("Greated than second top score and less than 100");
////        }

        //  ' | ' is a bitwise OR, not the same as the logical OR ('||' called 'two pipes')


//          boolean isCar = false;
//          if (isCar){        // here we are using ONE '=' operator, which is different than '==' operator
//              System.out.println("this is not supposed to happen");
//          }
//
//
//          //    TERNARY OPERATOR
//        //  int ageOfClient = 20;
//        // boolean isEighteenOrOver = ageOfClient == 20? true : false;
//
//          boolean wasCar = isCar ? true : false;
//          if (wasCar) {
//              System.out.println("wasCar is true");
//          }












    }
}
